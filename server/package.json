{
  "name": "mustard-task-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mustard's hosted task control plane API (ADR-0013). Deployed as a Supabase Edge Function.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
